<div class="w-container{{normalClassName}}">
  {{#if isPrimary}}
    {{ui-logo label "index" svg=svg class="logo"}}
  {{/if}}

  {{yield (hash menu=(component "ui-navigation/container/menu"))}}
</div>
